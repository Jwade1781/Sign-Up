

window.onload = () => {
    createClouds();
}


function createClouds(){
    const TOTAL = 3;
    const pictureContainer = document.querySelector(".picture");

    /*for (let i = 0; i < TOTAL; i++) {
        let cloud = document.createElement("div");
        cloud.classList.add("cloud");
        let rotate = Math.random(1) * 5;
        cloud.style.transform = ("rotate(" + rotate + "deg");
        cloud.style.transform = ("scale(" + rotate + ")")
        pictureContainer.appendChild(cloud);
        console.log("Added cloud" + rotate);
    }*/
}

function events () {
    
}